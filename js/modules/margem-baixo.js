export default function initMargemBaixo() {
    const sections = document.querySelectorAll('.secao');

    const section = sections[sections.length - 1];

    section.style.marginBottom = '60px';
}